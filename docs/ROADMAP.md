# ZWX Agent 模块待办与进度

> 最后核对：2026-08-18。此文档是项目功能规划的单一事实来源。
>
> 状态含义：`已完成` 表示仓库中已有对应实现；`进行中` 表示已有基础实现但尚未达到可稳定交付的标准；`待做` 表示当前未找到实现。进度按本模块下已完成项 / 全部项计算，避免把设想当作既有能力。

## 总览

| 模块 | 进度 | 当前状态 | 下一步 |
| --- | ---: | --- | --- |
| RAG 与知识库 | 50% (4/8) | 进行中 | 建立评测与混合检索 |
| 联网查询与网页处理 | 50% (3/6) | 进行中 | 加来源引用与安全策略 |
| 工具调用与执行 | 57% (4/7) | 进行中 | 权限、沙箱与审批 |
| Skill 体系 | 71% (5/7) | 进行中 | 加入权限上下文与更多受控 Skill |
| MCP 集成 | 33% (1/3) | 进行中 | 统一发现、配置与健康检查 |
| 智能体编排 | 50% (3/6) | 进行中 | 规划、恢复与质量控制 |
| 会话与记忆 | 67% (2/3) | 进行中 | 长期记忆策略 |
| 可观测性与评测 | 40% (2/5) | 进行中 | 端到端评测集与指标面板 |
| 安全、租户与治理 | 40% (2/5) | 进行中 | 服务端认证与细粒度授权 |
| 前端体验与运维 | 50% (3/6) | 进行中 | 失败恢复与部署监控 |

## RAG 与知识库

- [x] `已完成` 私有知识文档按 `tenantId`、`agentKey` 隔离检索。
- [x] `已完成` Markdown 感知切分、异步索引任务与 PGVector 存储。
- [x] `已完成` 检索调用链、候选片段与引用回传。
- [x] `已完成` 检索超时/异常降级，不阻断模型回答。
- [ ] `待做` 为业务问题建立带标准答案和证据的 RAG 评测集。
- [ ] `待做` 实现关键词 + 向量的混合召回与重排序，并对比评测结果。
- [ ] `待做` 补齐 PDF、Office、表格、图片/OCR 的统一解析和质量校验。
- [ ] `待做` 建立文档版本、删除、重建索引和过期内容的生命周期管理。

## 联网查询与网页处理

- [x] `已完成` 已提供联网搜索工具，并可由旅游规划与测试智能体按轮次启用。
- [x] `已完成` 已提供网页抓取与资源下载工具。
- [x] `已完成` 工具调用过程可写入旅行规划的执行事件。
- [ ] `待做` 将搜索结果的 URL、标题、抓取时间作为用户可见引用返回。
- [ ] `待做` 增加域名白/黑名单、SSRF 防护、重定向限制和内容大小限制。
- [ ] `待做` 对搜索、抓取失败建立重试、缓存、限流和可观测指标。

## 工具调用与执行

- [x] `已完成` 已注册文件、终端、PDF、搜索、抓取、下载与终止工具。
- [x] `已完成` 智能体可在开启联网时获得受限工具回调。
- [x] `已完成` 旅行规划可记录检索、工具调用、生成和持久化执行事件。
- [x] `已完成` 已有工具单元测试覆盖。
- [ ] `待做` 按智能体、租户和用户角色制定工具授权策略。
- [ ] `待做` 将文件与终端操作迁入隔离工作目录/沙箱，并限制命令与资源配额。
- [ ] `待做` 对有副作用的工具增加用户确认、审计日志和可取消机制。

## Skill 体系

当前实现和触发规则见 [skills.md](skills.md)。首个受控 Skill 为 `web-research`；现有 Java 工具与 MCP 服务不能自动等同于 Skill。

- [x] `已完成` 定义首个 Skill 的标识、说明、触发条件和适用智能体元数据。
- [x] `已完成` 实现 Skill 注册表，按智能体和本轮联网授权筛选并注入工具回调。
- [x] `已完成` 将触发与拒绝规则注入系统提示词，且以未暴露工具回调作为强制边界。
- [x] `已完成` 提供按租户、智能体持久化的配置化启停，并在后续对话立即生效。
- [ ] `待做` 为 Skill 注入权限上下文、超时和资源限制。
- [x] `已完成` 提供 Web/桌面端共用的 Skill 配置入口、状态展示和保存反馈。
- [ ] `待做` 增加版本和输入输出元数据。
- [ ] `待做` 为每个 Skill 建立版本兼容、回滚及端到端契约测试。

## MCP 集成

- [x] `已完成` 存在图片搜索 MCP 独立子服务及连接配置。
- [ ] `待做` 在主服务实现 MCP Server 发现、连接管理和工具目录同步。
- [ ] `待做` 增加 MCP 健康检查、超时、鉴权和故障降级。

## 智能体编排

- [x] `已完成` 已有情感、旅游和测试三个智能体定义与能力标签。
- [x] `已完成` 支持流式回复、最近 20 条上下文与重试关联。
- [x] `已完成` 旅游规划具备 RAG + 可选联网工具的编排路径。
- [ ] `待做` 将任务拆解、计划、执行、验证和最终回答建模为显式状态机。
- [ ] `待做` 支持中断后的运行恢复、幂等重试和任务取消。
- [ ] `待做` 增加事实核验、证据不足拒答和答案质量门控。

## 会话与记忆

- [x] `已完成` 会话、消息、RAG 引用与视觉摘要已持久化。
- [x] `已完成` 前端支持会话历史创建、切换、删除和恢复展示。
- [ ] `待做` 实现可解释的长期记忆提取、更新、遗忘和用户编辑机制。

## 可观测性与评测

- [x] `已完成` RAG trace 和旅行规划执行事件已落库并可查询。
- [x] `已完成` 已有 RAG、智能体与工具的基础单元测试。
- [ ] `待做` 为 RAG、工具选择、任务成功率、延迟和成本定义统一指标。
- [ ] `待做` 建立离线回归评测及发布前门禁。
- [ ] `待做` 建立线上错误、超时、检索空命中和工具失败告警面板。

## 安全、租户与治理

- [x] `已完成` 私有知识库切片带租户与智能体元数据，并在检索时过滤。
- [x] `已完成` 图片与私有资料使用对象存储，读取使用短时签名 URL。
- [ ] `待做` 以服务端认证身份确定租户；不得信任客户端传入的 `tenantId`。
- [ ] `待做` 实现用户/角色级数据和工具授权，覆盖会话、文档和执行记录。
- [ ] `待做` 定义数据保留、删除、导出、脱敏和审计策略。

## 前端体验与运维

- [x] `已完成` 前端提供多智能体入口、SSE 对话、文件上传、联网开关、引用与执行状态展示。
- [x] `已完成` 已提供 Docker Compose 与桌面端打包脚本。
- [x] `已完成` 提供知识库管理页面。
- [ ] `待做` 补齐 SSE 断线恢复、请求取消、失败重试和明确错误提示。
- [ ] `待做` 为索引、MCP、模型、数据库与对象存储增加健康检查和部署监控。
- [ ] `待做` 为关键流程补充端到端测试，并覆盖权限和失败分支。

## 近期优先级

1. RAG 评测集 + 混合检索/重排序：先用数据确认优化是否有效。
2. 联网查询引用与安全边界：让实时信息可验证、可控。
3. 工具沙箱与授权：在扩大自动化能力前收紧副作用边界。
4. Skill 最小闭环：先做注册、启停、授权、选择和契约测试，再扩展 Skill 数量。
5. 统一评测与监控：把每次改动变成可量化的质量提升。
